export const EVENT_ID = '1y2026';
export const LOCAL_BEST_KEY = `juicy-merge-best-${EVENT_ID}`;

/** Score required to unlock the reward. */
export const SCORE_THRESHOLD = 4000;

/** The reward code players receive after reaching SCORE_THRESHOLD. */
export const REWARD_CODE = 'uEZRyCAMHS';
